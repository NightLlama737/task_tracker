# Task Tracker

## Požadavky
- Docker + Docker Compose
- Git

## Nasazení
```bash
git clone <https://github.com/NightLlama737/task_tracker.git>
cd task-tracker
docker-compose up --build -d
Přístup: Frontend na http://localhost:3000, RedisInsight na http://localhost:8001